module main

#name    = "fail_borrow"
#version = Version(1, 0, 0)
#build   = exe
#bitsize = 32

// cannot return a reference

struct Data {
    pub value: i32
}

func return_ref(d: &Data) &Data {
    return d
}

func main() void { }
