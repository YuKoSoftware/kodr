module fixture


// module-level var is not allowed (must be const) — parser catches first

var top_level: i32 = 10  //> [E2001]

// duplicate variable in same scope

func dup_in_scope() void {
    const x: i32 = 1
    const x: i32 = 2
}

func main() void { }
