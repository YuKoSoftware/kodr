module fixture

#version = (1, 0, 0)
#build   = exe

func check(x: i32) i32 {
    if(x > 0) {
        return 1
    } else if(x < 0) {  //> [E0101]
        return -1
    }
    return 0  //> [E0101]
}

func main() void { }
