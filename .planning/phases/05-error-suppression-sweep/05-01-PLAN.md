---
phase: 05-error-suppression-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codegen.zig
autonomous: true
requirements:
  - ESUP-01

must_haves:
  truths:
    - "The 4 compiler-side catch unreachable at lines 700, 726, 950, 983 are replaced with error-returning patterns"
    - "The 8 generated-code catch unreachable (lines 1854-1879, 2292-2314) remain untouched"
    - "Thread spawning codegen still produces valid Zig that compiles"
  artifacts:
    - path: "src/codegen.zig"
      provides: "Thread state allocation with error propagation instead of crash"
      contains: "orelse return error.OutOfMemory"
  key_links:
    - from: "src/codegen.zig"
      to: "generated Zig thread code"
      via: "emitFmt for thread shared state"
      pattern: "page_allocator\\.create.*orelse"
---

<objective>
Replace the 4 compiler-side `catch unreachable` in codegen.zig with proper error propagation in generated Zig output.

Purpose: Thread shared state allocation currently crashes the user's program with unreachable on OOM instead of propagating errors. A "safe" language must not emit code that panics on allocation failure.

Output: codegen.zig emits `orelse return error.OutOfMemory` (or `catch return error.OutOfMemory`) instead of `catch unreachable` for the 4 thread state allocation sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-error-suppression-sweep/05-CONTEXT.md
@src/codegen.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 4 catch unreachable with error propagation in thread codegen</name>
  <files>src/codegen.zig</files>
  <read_first>src/codegen.zig lines 690-730, 940-990</read_first>
  <action>
Fix the 4 compiler-side `catch unreachable` instances per D-01 and D-04.

**Lines 700 and 950** — `page_allocator.create(SharedState) catch unreachable`:
Change the emitted string from `catch unreachable` to `catch return error.OutOfMemory`. The generated function already returns `anyerror!` (thread spawn wrapper), so returning an error is valid.

Specifically, change:
```
"const _state = std.heap.page_allocator.create({s}.SharedState) catch unreachable;\n"
```
to:
```
"const _state = std.heap.page_allocator.create({s}.SharedState) catch return error.OutOfMemory;\n"
```

**Lines 726 and 983** — `Thread.spawn(...) catch unreachable`:
Change the emitted string from `catch unreachable` to `catch |e| return e`. Thread.spawn returns `SpawnError` which is compatible with `anyerror!`.

Specifically, change:
```
" }) catch unreachable, .state = _state };\n"
```
to:
```
" }) catch |e| return e, .state = _state };\n"
```

**CRITICAL: DO NOT touch lines 1854-1879 or 2292-2314.** These emit `catch unreachable` in generated Zig for error union narrowing — this is correct behavior per D-02.

**DO NOT touch line 2579** — it is a comment per D-03.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && grep -n 'catch unreachable' src/codegen.zig | grep -v 'self.emit' | grep -v '///'</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'catch unreachable' src/codegen.zig` returns exactly 9 (was 13: minus 4 fixed, keeping 8 generated-code emits + 1 comment)
- Lines 700, 726, 950, 983 no longer contain `catch unreachable`
- Lines 1854-1879 and 2292-2314 are unchanged
- `./testall.sh` passes all stages
  </acceptance_criteria>
  <done>The 4 thread state allocation catch unreachable are replaced with proper error returns. Generated thread code propagates OOM and spawn errors instead of crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no test regressions and count remaining instances</name>
  <files>src/codegen.zig</files>
  <read_first>src/codegen.zig lines 700, 726, 950, 983 (verify changes), lines 1854-1879 (verify untouched)</read_first>
  <action>
Run the full test suite to confirm no regressions from the codegen changes.

1. Run `./testall.sh` and confirm all 11 stages pass.
2. Run `grep -c 'catch unreachable' src/codegen.zig` and confirm the count.
3. The expected count breakdown:
   - 6 instances of `self.emit(" catch unreachable")` on lines 1856, 1873, 1879, 2294, 2307, 2314 — generated error union narrowing (CORRECT, keep)
   - 2 instances of comments containing `catch unreachable` on lines 1854/1871/2292/2305 (comment lines) and 2579 — (CORRECT, keep)
   - 0 instances of `catch unreachable` in thread state allocation — (FIXED)

If any test fails, investigate whether the thread spawn wrapper function's return type needs adjustment. The functions emitting this code are `generateThreadFunc` (two copies: legacy AST path and MIR path). Both should produce functions returning `anyerror!ThreadType`.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- `./testall.sh` exits 0 with all 11 stages passing
- `grep -c 'catch unreachable' src/codegen.zig` output is consistent (no compiler-side catch unreachable remain outside of emit strings and comments)
  </acceptance_criteria>
  <done>Full test suite passes with no regressions. The codegen catch unreachable sweep is verified complete.</done>
</task>

</tasks>

<verification>
1. `grep -n 'catch unreachable' src/codegen.zig` shows only emit-string and comment instances
2. Lines 1854-1879 and 2292-2314 are byte-identical to before
3. `./testall.sh` passes all 11 stages
</verification>

<success_criteria>
- Zero compiler-side `catch unreachable` in codegen.zig
- All generated-code `catch unreachable` preserved unchanged
- All 11 test stages pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-error-suppression-sweep/05-01-SUMMARY.md`
</output>
