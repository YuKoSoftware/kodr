---
phase: 20-tamga-build-verification
plan: 03
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_loop.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.zig
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.zig
  - /home/yunus/Projects/orhon/tamga_framework/src/test/test_sdl3.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/test/test_vulkan.orh
  - /home/yunus/Projects/orhon/tamga_framework/src/main.orh
autonomous: false
requirements: [REQ-20]
must_haves:
  truths:
    - "Tamga framework builds end-to-end with orhon build — zero errors"
    - "All workarounds removed from Tamga source files"
    - "Orhon compiler test suite still passes after all changes"
    - "Bug 4 secondary cross-bridge conflict does not block the build"
  artifacts:
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh"
      provides: "Clean event union with null, typed enum fields, no sentinel struct"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh"
      provides: "size param names restored, #csource directive"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.zig"
      provides: "pub export fn declarations, shared vulkan_c import"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh"
      provides: "const &Mesh parameters, no by-value workaround"
    - path: "/home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.zig"
      provides: "shared vulkan_c import, no @ptrCast workarounds, no file-relative @import for bridge modules"
  key_links:
    - from: "tamga_sdl3.orh pollEvent return type"
      to: "codegen.zig typeToZig"
      via: "(null | QuitEvent | ...) -> ?(union(enum) {...})"
      pattern: "null \\|"
    - from: "tamga_vma.orh #csource"
      to: "zig_runner.zig addCSourceFiles"
      via: "#csource metadata -> build.zig emission"
      pattern: "#csource"
    - from: "tamga_vk3d.zig @import"
      to: "shared vulkan_c module"
      via: "@import(\"vulkan_c\") replacing @cImport"
      pattern: "vulkan_c"
---

<objective>
Remove all Tamga workarounds and verify end-to-end build with the fixed compiler.

Purpose: Per D-02 and D-04, the final verification is building Tamga from its repo with the newly-built compiler and confirming all workarounds are removed. This proves all 9 bugs are truly fixed. Bug 7 (export fn pub) is fixed here as a direct sidecar edit — the clean approach per CLAUDE.md.
Output: Clean Tamga source files with no workarounds, successful orhon build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-tamga-build-verification/20-CONTEXT.md
@.planning/phases/20-tamga-build-verification/20-RESEARCH.md
@.planning/phases/20-tamga-build-verification/20-01-SUMMARY.md
@.planning/phases/20-tamga-build-verification/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all Tamga workarounds per D-04 (including Bug 7 direct fix)</name>
  <files>
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_loop.orh
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.zig
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh
    /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.zig
    /home/yunus/Projects/orhon/tamga_framework/src/test/test_sdl3.orh
    /home/yunus/Projects/orhon/tamga_framework/src/test/test_vulkan.orh
    /home/yunus/Projects/orhon/tamga_framework/src/main.orh
  </files>
  <read_first>
    - /home/yunus/Projects/orhon/tamga_framework/docs/bugs.md (workaround descriptions for all 9 bugs)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_sdl3.orh (NoEvent sentinel, raw int fields, pollEvent return type)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaSDL3/tamga_loop.orh (event loop using NoEvent)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.orh (byte_size/byte_count param names)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVMA/tamga_vma.zig (export fn without pub, @cImport)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.orh (mesh by-value instead of const &Mesh)
    - /home/yunus/Projects/orhon/tamga_framework/src/TamgaVK3D/tamga_vk3d.zig (@cImport, @ptrCast workarounds)
    - /home/yunus/Projects/orhon/tamga_framework/src/test/test_sdl3.orh (raw integer comparisons)
    - /home/yunus/Projects/orhon/tamga_framework/src/test/test_vulkan.orh (cross-module type dispatch)
    - /home/yunus/Projects/orhon/tamga_framework/src/main.orh (bridge func declarations)
    - .planning/phases/20-tamga-build-verification/20-01-SUMMARY.md (what bugs were fixed in Plan 01)
    - .planning/phases/20-tamga-build-verification/20-02-SUMMARY.md (what bugs were fixed in Plan 02)
  </read_first>
  <action>
**Pre-check: Bug 4 secondary cross-bridge conflict probe (RESEARCH.md Pitfall 2)**
Before making any workaround changes, run a quick `orhon build` probe in the Tamga directory to identify the current error state. Specifically check for:
- The "file exists in modules 'tamga_vma' and 'root'" error for `tamga_vk3d_bridge.zig` (documented in RESEARCH.md Pitfall 2)
- Whether `tamga_vk3d.zig` uses file-relative `@import("tamga_vma_bridge")` which causes module membership conflicts

If the Bug 4 secondary conflict appears:
- Check whether `tamga_vk3d.zig` has a file-relative `@import` for bridge modules (e.g., `@import("tamga_vma_bridge")` or `@import("tamga_vk3d_bridge.zig")`)
- If so, change it to use the named module import that the compiler already wires via `addImport` in the generated `build.zig` (the named import should already be available per the Bug 4 fix from the debug session)
- Document what was found and how it was resolved (or if it was already resolved)

Record the probe results before proceeding with workaround removal.

**Bug 1+3 workaround removal (tamga_sdl3.orh):**
- Remove the `NoEvent` sentinel struct entirely (the struct with `pub empty: bool` dummy field)
- Change `pollEvent` return type from `(NoEvent | QuitEvent | KeyDownEvent | ...)` to `(null | QuitEvent | KeyDownEvent | ...)`
- Change all `return NoEvent(empty: false)` to `return null`
- Update any callers that check `is not tamga_sdl3.NoEvent` to check `== null` or handle the nullable union

**Bug 2 workaround removal (tamga_sdl3.orh):**
- Change `scancode: u32` to `scancode: Scancode` in KeyDownEvent struct
- Change `button: u8` to `button: MouseButton` in MouseButtonEvent struct (if applicable)
- In pollEvent where raw int fields are populated, use `cast(Scancode, raw_value)` instead of raw assignment

**Bug 2 workaround removal (test_sdl3.orh, test_vulkan.orh):**
- Change `kd.scancode == 41` to `kd.scancode == tamga_sdl3.Scancode.Escape` (or the appropriate enum comparison)
- Remove any other raw integer comparisons that should use typed enums

**Bug 5 workaround removal (tamga_vma.orh):**
- Rename `byte_size` parameters back to `size` in bridge func declarations
- Rename `byte_count` parameters back to `size` where applicable

**Bug 6 workaround removal (tamga_vk3d.orh):**
- Change `mesh: Mesh` to `mesh: const &Mesh` in bridge func draw/destroyMesh declarations
- The compiler now injects address-of at call sites for const & bridge params

**Bug 7 direct fix (tamga_vma.zig):**
Change all 5 `export fn` declarations to `pub export fn` directly in the sidecar file. Per RESEARCH.md, the affected lines are 426, 445, 450, 464, 470 in `tamga_vma.zig`. This is the clean fix — no compiler-side regex post-processing needed. The compiler copies sidecars verbatim, and `pub export fn` is the correct Zig form for bridge-exported functions.

NOTE: Do NOT add compiler-side string replacement for `export fn` -> `pub export fn`. That would violate CLAUDE.md's "No hacky workarounds" and "No post-processing sidecar files with regex" rules. The sidecar source itself should be correct.

**Bug 8 workaround removal (tamga_vk3d.zig, tamga_vma.zig):**
- In `tamga_vk3d.zig`: replace `const c = @cImport({ @cInclude("vulkan/vulkan.h"); })` with `const c = @import("vulkan_c")`
- In `tamga_vma.zig`: replace `const c = @cImport({ @cInclude("vulkan/vulkan.h"); })` with `const c = @import("vulkan_c")`
- Remove ALL `@ptrCast` calls at cross-module Vulkan handle boundaries in tamga_vk3d.zig (these were needed because each module had its own @cImport creating incompatible types)
- Add `#cInclude "vulkan/vulkan.h"` metadata to tamga_vma.orh and tamga_vk3d.orh (so the compiler generates the shared vulkan_c module)
- Ensure `tamga_vk3d.zig` does NOT use file-relative `@import` for bridge modules — use named module imports only (addresses Pitfall 2)

**Bug 9 workaround removal (tamga_vma.orh):**
- Add `#csource "vma_impl.cpp"` to tamga_vma.orh module metadata
- Add `#linkCpp` to tamga_vma.orh module metadata (if needed for C++ linking)
- The pre-compiled `vma_impl.o` and manual build.zig patching are no longer needed

**Bug 4 (already fixed):** No workaround removal needed — the fix was compiler-side only. The secondary cross-bridge conflict is addressed by the pre-check above.

After all changes, do a `git diff` in the Tamga directory to verify only intended lines changed per CLAUDE.md workflow rules.
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/tamga_framework && git diff --stat</automated>
  </verify>
  <acceptance_criteria>
    - tamga_sdl3.orh does NOT contain "NoEvent" struct definition
    - tamga_sdl3.orh pollEvent return type contains "null |" (not "NoEvent |")
    - tamga_sdl3.orh KeyDownEvent contains "scancode: Scancode" (not "scancode: u32")
    - tamga_vma.orh contains "size:" parameter (not "byte_size:" or "byte_count:")
    - tamga_vma.orh contains "#csource" directive
    - tamga_vma.zig contains "pub export fn" (not bare "export fn") for all 5 export declarations
    - tamga_vk3d.orh contains "const &Mesh" (not "mesh: Mesh" by-value)
    - tamga_vk3d.zig contains `@import("vulkan_c")` (not `@cImport`)
    - tamga_vk3d.zig does NOT contain file-relative `@import` for bridge modules (no `@import("tamga_vma_bridge")` string-path style that causes Pitfall 2 conflict)
    - tamga_vma.zig contains `@import("vulkan_c")` (not `@cImport`)
    - test_sdl3.orh does NOT contain raw integer scancode comparisons like "== 41"
    - Bug 4 secondary cross-bridge conflict ("file exists in modules") either confirmed absent or resolved
  </acceptance_criteria>
  <done>All 8 workarounds removed from Tamga source files. Bug 7 fixed directly in sidecar. Bug 4 secondary conflict verified clear. Code is clean and uses the fixed compiler features directly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Tamga build verification</name>
  <files>none</files>
  <action>
Build the compiler with `zig build`, run `./testall.sh` to confirm no regressions (per D-03), then build Tamga with `orhon build` in the Tamga directory (per D-02). Present results to user for verification.

Steps:
1. cd /home/yunus/Projects/orhon/orhon_compiler && zig build
2. ./testall.sh — all 11 stages must pass
3. cd /home/yunus/Projects/orhon/tamga_framework && /home/yunus/Projects/orhon/orhon_compiler/zig-out/bin/orhon build
4. Report success or failure to user
5. If any "file exists in modules" error appears (Bug 4 secondary conflict), report the exact error and which files are involved
  </action>
  <verify>
    <automated>cd /home/yunus/Projects/orhon/orhon_compiler && ./testall.sh 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms Tamga builds end-to-end with zero errors. All 9 bugs verified fixed. No Bug 4 secondary conflicts. Phase 20 complete.</done>
</task>

</tasks>

<verification>
- All Tamga workarounds removed (per D-04)
- Bug 7 fixed directly in tamga_vma.zig (pub export fn) — no compiler-side regex
- Bug 4 secondary cross-bridge conflict probed and resolved (per RESEARCH.md Pitfall 2)
- `./testall.sh` passes in compiler repo (per D-03)
- `orhon build` succeeds in Tamga repo (per D-02)
- Generated build.zig contains correct cImport modules, csource, pub export fn
</verification>

<success_criteria>
Tamga framework builds end-to-end with the new bridge module system. No workarounds needed. All 9 bugs confirmed fixed by successful Tamga compilation.
</success_criteria>

<output>
After completion, create `.planning/phases/20-tamga-build-verification/20-03-SUMMARY.md`
</output>
