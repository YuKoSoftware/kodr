module http

// http — HTTP client for Orhon programs
// Import with: import std::http

// Send a GET request, return response body

pub bridge func get(url: String) ErrorUnion(String)

// Send a POST request with body and content type, return response body

pub bridge func post(url: String, body: String, contentType: String) ErrorUnion(String)

// ── URL Parsing ──

// Get the scheme (http, https)

pub bridge func urlScheme(url: String) ErrorUnion(String)

// Get the host

pub bridge func urlHost(url: String) ErrorUnion(String)

// Get the port (returns 0 if not specified)

pub bridge func urlPort(url: String) ErrorUnion(i32)

// Get the path

pub bridge func urlPath(url: String) ErrorUnion(String)

// Get the query string (without leading ?)

pub bridge func urlQuery(url: String) ErrorUnion(String)

// Build a URL from components

pub bridge func urlBuild(scheme: String, host: String, port: i32, path: String, query: String) String
