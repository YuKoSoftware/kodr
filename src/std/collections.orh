module collections

// collections — generic data structures for Orhon programs
// Types are used directly: List(i32), Map(String, i32), Set(String)
// Auto-imported by the compiler — no explicit import needed.

// List(T) — dynamic array

// Map(K, V) — hash map

// Set(T) — hash set
